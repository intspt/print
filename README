print

环境 
python 2.7(.9)

pip install -r requirements.txt

config.py中有一些可以更改的配置
使用前请运行init.py初始化数据库

每行队伍信息顺序是:
队伍名 密码 座位信息
例如
team1   pass1   601-29
中间用【制表符】隔开

座位信息中均不能包含空格
座位信息中均不能包含空格
座位信息中均不能包含空格
因为很重要所以说三遍
因为很重要所以说三遍
因为很重要所以说三遍